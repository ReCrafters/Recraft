<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Seller Dashboard</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(to right, #c6a98d, #f1dfc7);
    }

    header {
      background-color: #1e1e1e;
      color: #fff;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      font-size: 24px;
      margin: 0;
    }

    nav a {
      color: #fff;
      text-decoration: none;
      margin-left: 25px;
      font-weight: 500;
    }

    .dashboard-container {
      padding: 40px;
    }

    .welcome-box {
      background-color: #e8d3b9;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      margin-bottom: 30px;
    }

    .welcome-box h2 {
      font-size: 28px;
      margin: 0 0 10px 0;
      color: #3b2a1a;
    }

    .welcome-box p {
      color: #5e4736;
      font-size: 15px;
    }

    .product-section {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 25px;
    }

    .product-card {
      background-color: #f5e7d7;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 6px 12px rgba(0,0,0,0.08);
      text-align: center;
      transition: transform 0.2s ease;
    }

    .product-card:hover {
      transform: scale(1.02);
    }

    .product-card img {
      max-width: 100%;
      height: 160px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 15px;
    }

    .product-card h3 {
      margin: 0 0 8px;
      color: #3a2d1f;
      font-size: 18px;
    }

    .product-card p {
      color: #6a5848;
      font-size: 14px;
    }

    .no-products {
      text-align: center;
      font-size: 16px;
      color: #6e5644;
      grid-column: 1 / -1;
    }
  </style>
</head>
<body>

  <header>
    <h1>ReCraft Seller</h1>
    <nav>
      <a href="/add-product">Add Product</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>

  <div class="dashboard-container">
    <div class="welcome-box">
      <h2>Welcome, <%= seller.name %>!</h2>
      <p>Manage your uploaded eco-friendly products and track your impact.</p>
    </div>

    <div class="product-section">
      <% if (products.length === 0) { %>
        <p class="no-products">You haven't uploaded any products yet.</p>
      <% } else { %>
        <% products.forEach(product => { %>
          <div class="product-card">
            <img src="<%= product.imageURL %>" alt="<%= product.name %>">
            <h3><%= product.name %></h3>
            <p><%= product.description %></p>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>

</body>
</html>
